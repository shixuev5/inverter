{
  "name": "inverter-control-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for inverter status query and control",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "keywords": ["cloudflare", "worker", "inverter", "growatt"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
